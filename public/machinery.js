(() => {
  let machineMe=null,machineUsers=[],machines=[],machineTasks=[],selectedMachineId=null,machineNameFilter='',machineSort='asc',taskSort={key:'title',direction:'asc'},taskFilters={title:'',creator:'',dueDate:'',status:''};
  const q=s=>document.querySelector(s),qa=s=>[...document.querySelectorAll(s)];
  const esc=v=>{const n=document.createElement('span');n.textContent=v||'';return n.innerHTML;};
  const api=async(url,options={})=>{const r=await fetch(url,{headers:{'Content-Type':'application/json'},...options}),d=await r.json();if(!r.ok)throw new Error(d.error||'No se pudo completar la operación');return d;};
  const toast=m=>{const n=q('#toast');n.textContent=m;n.classList.add('show');setTimeout(()=>n.classList.remove('show'),2600);};
  const date=v=>{if(!v)return 'Sin fecha';const[y,m,d]=v.split('-');return`${d}/${m}/${y}`;};
  const byName=(a,b)=>a.name.localeCompare(b.name,'es',{sensitivity:'base'});

  async function loadMachinery(){try{const[meData,userData,machineData,taskData]=await Promise.all([api('/api/me'),api('/api/users'),api('/api/machines'),api('/api/machine-tasks')]);machineMe=meData.user;machineUsers=userData.users.sort(byName);machines=machineData.machines.sort(byName);machineTasks=taskData.machineTasks;selectedMachineId=history.state?.view==='machines'?(history.state.machineId||null):null;renderIndex();if(selectedMachineId&&machines.some(x=>x.id===selectedMachineId)){showDetail();renderDetail(selectedMachineId);}else{selectedMachineId=null;showIndex();}fillResponsibleOptions();}catch(error){toast(error.message);}}
  window.loadMachinery=loadMachinery;
  window.openMachineFromHistory=id=>{selectedMachineId=id;if(id&&machines.some(x=>x.id===id)){showDetail();renderDetail(id);}else{selectedMachineId=null;showIndex();}};

  function renderIndex(){
    const visible=machines.filter(machine=>machine.name.toLowerCase().includes(machineNameFilter.toLowerCase())).sort((a,b)=>machineSort==='asc'?byName(a,b):-byName(a,b));
    const rows=visible.map(machine=>`<tr><td><button class="machine-link" data-id="${machine.id}">${esc(machine.name)}</button></td><td>${machineMe.role==='admin'?`<button class="edit-machine" data-id="${machine.id}">Modificar</button> <button class="delete-machine danger" data-id="${machine.id}">Eliminar</button>`:'—'}</td></tr>`).join('');
    const add=machineMe.role==='admin'?'<tr class="add-machine-row"><td colspan="2"><button id="newMachine" type="button">＋ Agregar nueva maquinaria</button></td></tr>':'';
    q('#machineList').innerHTML=`<div class="machine-index-wrap"><table class="machine-index-table"><thead><tr><th>Nombre del equipo <button id="sortMachines" class="excel-sort" type="button">${machineSort==='asc'?'↑':'↓'}</button></th><th>Acciones</th></tr><tr class="excel-filter-row"><th><input id="machineNameFilter" value="${esc(machineNameFilter)}" placeholder="Filtrar por nombre"></th><th><button id="clearMachineFilter" type="button">Limpiar filtro</button></th></tr></thead><tbody>${rows||'<tr><td colspan="2" class="machine-empty">No hay maquinarias que coincidan con el filtro.</td></tr>'}${add}</tbody></table></div>`;
    qa('.machine-link').forEach(b=>b.addEventListener('click',()=>openDetail(b.dataset.id)));qa('.edit-machine').forEach(b=>b.addEventListener('click',()=>openMachine(b.dataset.id)));qa('.delete-machine').forEach(b=>b.addEventListener('click',()=>deleteMachine(b.dataset.id)));q('#newMachine')?.addEventListener('click',()=>openMachine());q('#sortMachines').addEventListener('click',()=>{machineSort=machineSort==='asc'?'desc':'asc';renderIndex();});q('#machineNameFilter').addEventListener('change',e=>{machineNameFilter=e.target.value;renderIndex();});q('#clearMachineFilter').addEventListener('click',()=>{machineNameFilter='';renderIndex();});
  }

  function openDetail(id){selectedMachineId=id;history.pushState({view:'machines',machineId:id},'',`#machines/${id}`);showDetail();renderDetail(id);}
  function showIndex(){q('#machineIndex').classList.remove('hidden');q('#machineDetail').classList.add('hidden');}
  function showDetail(){q('#machineIndex').classList.add('hidden');q('#machineDetail').classList.remove('hidden');}
  function renderDetail(id){
    const machine=machines.find(x=>x.id===id);if(!machine)return showIndex();let tasks=machineTasks.filter(x=>x.machineId===id).filter(task=>(!taskFilters.title||task.title.toLowerCase().includes(taskFilters.title.toLowerCase()))&&(!taskFilters.creator||task.creatorName===taskFilters.creator)&&(!taskFilters.dueDate||task.dueDate===taskFilters.dueDate)&&(!taskFilters.status||(taskFilters.status==='done'?task.progress===100:task.progress!==100)));
    tasks.sort((a,b)=>{let l=a[taskSort.key]??'',r=b[taskSort.key]??'';if(taskSort.key==='status'){l=a.progress===100?1:0;r=b.progress===100?1:0;}const result=String(l).localeCompare(String(r),'es',{numeric:true,sensitivity:'base'});return taskSort.direction==='asc'?result:-result;});
    const creators=[...new Set(machineTasks.filter(x=>x.machineId===id).map(x=>x.creatorName))].sort((a,b)=>a.localeCompare(b,'es'));
    const sortButton=key=>`<button class="task-sort excel-sort" data-key="${key}" type="button">${taskSort.key===key?(taskSort.direction==='asc'?'↑':'↓'):'↕'}</button>`;
    q('#machineDetail').innerHTML=`<div class="machine-detail-head"><div><button id="backMachines" class="back-machines" type="button">← Maquinarias</button><h2>${esc(machine.name)}</h2><p>Responsable: <strong>${esc(machine.responsibleName)}</strong></p></div></div><div class="excel-sheet-label"><span>TAREAS DEL EQUIPO</span><button id="addMachineTask" type="button">＋ Agregar tarea</button></div><div class="machine-table-wrap"><table class="machine-table"><thead><tr><th>Tarea ${sortButton('title')}</th><th>Descripción</th><th>Creada por ${sortButton('creatorName')}</th><th>Terminación ${sortButton('dueDate')}</th><th>Estado ${sortButton('status')}</th><th>Acciones</th></tr><tr class="excel-filter-row"><th><input data-task-filter="title" value="${esc(taskFilters.title)}" placeholder="Filtrar"></th><th></th><th><select data-task-filter="creator"><option value="">Todos</option>${creators.map(x=>`<option ${taskFilters.creator===x?'selected':''}>${esc(x)}</option>`).join('')}</select></th><th><input data-task-filter="dueDate" type="date" value="${taskFilters.dueDate}"></th><th><select data-task-filter="status"><option value="">Todos</option><option value="pending" ${taskFilters.status==='pending'?'selected':''}>Pendiente</option><option value="done" ${taskFilters.status==='done'?'selected':''}>Finalizada</option></select></th><th><button id="clearTaskFilters" type="button">Limpiar</button></th></tr></thead><tbody>${tasks.length?tasks.map(taskRow).join(''):'<tr><td colspan="6" class="machine-empty">No hay tareas que coincidan con los filtros.</td></tr>'}</tbody></table></div>`;
    q('#backMachines').addEventListener('click',()=>history.back());q('#addMachineTask').addEventListener('click',()=>openTask('',id));qa('.finish-machine-task').forEach(b=>b.addEventListener('click',()=>finishTask(b.dataset.id)));qa('.edit-machine-task,.machine-task-title').forEach(b=>b.addEventListener('click',()=>openTask(b.dataset.id,id)));qa('.delete-machine-task').forEach(b=>b.addEventListener('click',()=>deleteTask(b.dataset.id)));qa('.machine-note').forEach(b=>b.addEventListener('click',()=>openNote(b.dataset.id)));qa('.task-sort').forEach(b=>b.addEventListener('click',()=>{taskSort=taskSort.key===b.dataset.key?{key:b.dataset.key,direction:taskSort.direction==='asc'?'desc':'asc'}:{key:b.dataset.key,direction:'asc'};renderDetail(id);}));qa('[data-task-filter]').forEach(c=>c.addEventListener('change',()=>{taskFilters[c.dataset.taskFilter]=c.value;renderDetail(id);}));q('#clearTaskFilters').addEventListener('click',()=>{taskFilters={title:'',creator:'',dueDate:'',status:''};renderDetail(id);});
  }

  function taskRow(task){
    const latest=(task.notes||[]).length?task.notes[task.notes.length-1].text:task.description||'Sin descripción',history=[task.description,...(task.notes||[]).map(n=>n.text)].filter(Boolean).join('\n\n'),canEdit=task.creatorId===machineMe.id||machineMe.role==='admin',canNote=task.creatorId===machineMe.id||task.assigneeId===machineMe.id;
    const finish=task.progress===100?'<span class="finished-label">Finalizada</span>':task.assigneeId===machineMe.id?`<button class="finish-machine-task" data-id="${task.id}">Finalizar</button>`:'Pendiente';
    return `<tr class="${task.progress===100?'machine-task-done':''}"><td>${canEdit&&task.progress!==100?`<button class="machine-task-title" data-id="${task.id}" title="Modificar tarea">${esc(task.title)}</button>`:`<strong>${esc(task.title)}</strong>`}</td><td class="machine-description"><details><summary>${esc(latest)}</summary><div class="history">${esc(history)}</div></details></td><td>${esc(task.creatorName)}</td><td>${date(task.dueDate)}</td><td>${finish}</td><td class="machine-task-actions">${canEdit&&task.progress!==100?`<button class="edit-machine-task" data-id="${task.id}">Modificar</button>`:''}${canNote&&task.progress!==100?`<button class="machine-note" data-id="${task.id}">Comentar</button>`:''}${machineMe.role==='admin'?`<button class="delete-machine-task" data-id="${task.id}">Eliminar</button>`:''}</td></tr>`;
  }

  function fillResponsibleOptions(){q('#machineForm select[name="responsibleId"]').innerHTML='<option value="">Selecciona un empleado</option>'+machineUsers.filter(x=>x.active).map(x=>`<option value="${x.id}">${esc(x.name)}</option>`).join('');}
  function openMachine(id=''){const f=q('#machineForm');f.reset();f.elements.id.value=id;f.querySelector('.formMessage').textContent='';if(id){const m=machines.find(x=>x.id===id);f.elements.name.value=m.name;f.elements.responsibleId.value=m.responsibleId;}q('#machineModal').classList.remove('hidden');}
  function openTask(id='',machineId=selectedMachineId){const f=q('#machineTaskForm');f.reset();f.elements.id.value=id;f.elements.machineId.value=machineId;f.querySelector('.formMessage').textContent='';q('#machineTaskModalTitle').textContent=id?'Modificar tarea de maquinaria':'Nueva tarea de maquinaria';if(id){const t=machineTasks.find(x=>x.id===id);f.elements.title.value=t.title;f.elements.description.value=t.description||'';f.elements.dueDate.value=t.dueDate;}q('#machineTaskModal').classList.remove('hidden');}
  function openNote(id){const f=q('#machineNoteForm');f.reset();f.elements.id.value=id;f.querySelector('.formMessage').textContent='';q('#machineNoteModal').classList.remove('hidden');}
  const close=id=>q('#'+id).classList.add('hidden');

  async function deleteMachine(id){const m=machines.find(x=>x.id===id);if(!confirm(`¿Eliminar ${m.name} y todas sus tareas?`))return;try{await api(`/api/machines/${id}`,{method:'DELETE'});selectedMachineId=null;await loadMachinery();toast('Maquinaria eliminada');}catch(error){toast(error.message);}}
  async function finishTask(id){if(!confirm('¿Confirmas que esta tarea fue finalizada?'))return;try{await api(`/api/machine-tasks/${id}/status`,{method:'PATCH',body:JSON.stringify({progress:100})});await loadMachinery();toast('Tarea finalizada');}catch(error){toast(error.message);}}
  async function deleteTask(id){if(!confirm('¿Eliminar esta tarea de maquinaria definitivamente?'))return;try{await api(`/api/machine-tasks/${id}`,{method:'DELETE'});await loadMachinery();toast('Tarea eliminada');}catch(error){toast(error.message);}}

  qa('[data-close]').forEach(b=>b.addEventListener('click',()=>close(b.dataset.close)));
  q('#machineForm').addEventListener('submit',async e=>{e.preventDefault();const f=e.target,id=f.elements.id.value;try{await api(id?`/api/machines/${id}`:'/api/machines',{method:id?'PATCH':'POST',body:JSON.stringify({name:f.elements.name.value,responsibleId:f.elements.responsibleId.value})});close('machineModal');await loadMachinery();toast(id?'Maquinaria modificada':'Maquinaria agregada');}catch(error){f.querySelector('.formMessage').textContent=error.message;}});
  q('#machineTaskForm').addEventListener('submit',async e=>{e.preventDefault();const f=e.target,id=f.elements.id.value,payload={machineId:f.elements.machineId.value,title:f.elements.title.value,description:f.elements.description.value,dueDate:f.elements.dueDate.value};try{await api(id?`/api/machine-tasks/${id}`:'/api/machine-tasks',{method:id?'PATCH':'POST',body:JSON.stringify(payload)});close('machineTaskModal');await loadMachinery();toast(id?'Tarea modificada':'Tarea agregada');}catch(error){f.querySelector('.formMessage').textContent=error.message;}});
  q('#machineNoteForm').addEventListener('submit',async e=>{e.preventDefault();const f=e.target;try{await api(`/api/machine-tasks/${f.elements.id.value}/notes`,{method:'POST',body:JSON.stringify({text:f.elements.text.value})});close('machineNoteModal');await loadMachinery();toast('Observación agregada');}catch(error){f.querySelector('.formMessage').textContent=error.message;}});
})();
